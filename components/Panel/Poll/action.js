import { AREAPOLL, DISTRICTPOLL, STATEPOLL, COUNTRYPOLL } from "./constant";

import { service } from "apiConnect";
import authSession from "utils/authSession";

const prefetchPollData = e => {
  return dispatch => {};
};

export default {
  prefetchPollData
};
